import GCheckboxButton from '../checkbox/src/checkbox-button.vue';

GCheckboxButton.install = function(Vue) {
  Vue.component(GCheckboxButton.name, GCheckboxButton);
};

export default GCheckboxButton;
